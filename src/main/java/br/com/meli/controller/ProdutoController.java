package br.com.meli.controller;

import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
@RequestMapping("/api/v1")
public class ProdutoController {

	// @Autowired
	// private ProdutoService produtoService;
	//
	/**
	 * @param
	 * @return ResponseEntitu<< List>ProdutoDTO>>
	 * @author thiago campos
	 */
	// @GetMapping("/category")
	// public ResponseEntity<List<Produto>> getListByCategory(@RequestParam String
	// categoryName) {
	// List<Produto> filteredProducts =
	// produtoService.filterByCategory(categoryName);
	// return ResponseEntity.ok(filteredProducts);
	// }
}
